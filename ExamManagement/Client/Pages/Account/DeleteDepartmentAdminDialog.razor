@inherits DeleteDepartmentAdminDialogBase

<MudDialog DisableSidePadding="true">
    <DialogContent>
            <MudContainer>
                <MudText>Last Name: @info.SurName</MudText>
                <MudText>First Name: @info.GivenName</MudText>
                <MudText>User Name: @info.UserName</MudText>
                <MudText>Email Address: @info.EmailAddress</MudText>
                <MudCardActions>
                    <MudSpacer />
                    <MudButton Style="margin-right:4px;" ButtonType="ButtonType.Reset" OnClick="()=>MudDialog.Cancel()" Variant="Variant.Filled" Color="Color.Dark">Cancel</MudButton>
                    <MudButton ButtonType="ButtonType.Submit" Disabled="DeleteButton" OnClick="DeleteDepartmentAdmin" Variant="Variant.Filled" Color="Color.Error">
                        @if (DeleteButton)
                        {
                            <MudProgressCircular Class="ms-n1" Size="Size.Small" Indeterminate="true" />
                            <MudText Class="ms-2">Deleting Department Admin</MudText>
                        }
                        else
                        {
                            <MudText>Delete Department Admin</MudText>
                        }
                    </MudButton>
                </MudCardActions>

            </MudContainer>
    </DialogContent>
</MudDialog>